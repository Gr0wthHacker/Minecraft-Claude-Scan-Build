{
	"schemaVersion": 1,
	"id": "chunkscan",
	"version": "${version}",
	"name": "ChunkScan",
	"description": "Client-side world capture and build helper: /cscan scans loaded chunks (or a Litematica selection) to a .litematic + .scan.json, places designs in Litematica at their recorded origin, marks named coordinates, highlights a design's dig list, auto-scans on a timer, and indexes every container you open so you can find items by name.",
	"authors": ["Jack Gierlich"],
	"license": "MIT",
	"environment": "client",
	"entrypoints": {
		"client": [
			"dev.jack.chunkscan.ChunkScanClient"
		]
	},
	"suggests": {
		"litematica": "*"
	},
	"depends": {
		"fabricloader": ">=0.19.3",
		"minecraft": "~26.2",
		"java": ">=25",
		"fabric-api": "*"
	}
}
